# Frontend Data Siswa

## Instalasi
1. `npm install`
2. Pastikan backend berjalan di `http://localhost:5000`.
3. Jalankan `npm run dev`.
4. Buka `http://localhost:3000`.

Frontend menggunakan React, Bootstrap, dan Axios.